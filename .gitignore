# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
.next/
out/
build/
*.tsbuildinfo

# Turbo
.turbo/

# Env
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Test/coverage
coverage/
.nyc_output/

# Misc
*.tgz
.cache/

# Claude (local-only — project settings.json + hooks are checked in)
.claude/settings.local.json
.claude/skills/
.agents/
.tmp

# Expo (machine-specific dev session state — created when `expo start` runs at repo root
# instead of from apps/mobile/, where the workspace .gitignore covers it)
.expo/

# Multi-agent coordination briefs (Planner/Implementor/Reviewer scratch; not deliverables)
.nex-tasks/

# Stray root tsconfig — Turborepo monorepo has per-workspace tsconfigs; root-level one
# is an accidental artifact from editors/CLI tools running in the wrong dir
/tsconfig.json
